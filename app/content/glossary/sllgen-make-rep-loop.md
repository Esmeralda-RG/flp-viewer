---
term: "sllgen:make-rep-loop"
title: "sllgen:make-rep-loop — construye el bucle REP"
relatedHelp: primer-interprete
---
Toma tres cosas — el prompt (`"--> "`), la función que evalúa cada programa (`eval-program`) y el parser (`scan&parse`) — y devuelve el bucle Read-Eval-Print completo que normalmente se guarda en `interpreter`.
