services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      RACKET_MAX_CONCURRENCY: "2"
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 2g
    restart: "no"
